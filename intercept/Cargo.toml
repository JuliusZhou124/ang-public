[package]
name = "intercept"
version = "0.1.0"
edition.workspace = true

[lib]
name = "intercept"
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"
serde = { workspace = true }
serde_json = { workspace = true }

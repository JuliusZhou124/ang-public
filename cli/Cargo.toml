[package]
name = "cli"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "ang"
path = "src/main.rs"

[dependencies]
runtime = { path = "../runtime" }
recorder = { path = "../recorder" }
replay = { path = "../replay" }
fault = { path = "../fault" }
uuid.workspace = true
anyhow.workspace = true

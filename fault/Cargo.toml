[package]
name = "fault"
version = "0.1.0"
edition.workspace = true

[dependencies]
recorder = { path = "../recorder" }
replay = { path = "../replay" }
rand = { workspace = true }

[dev-dependencies]
intercept = { path = "../intercept" }
